<UserControl x:Class="AutoActivator.Gui.Views.ComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             IsVisibleChanged="UserControl_IsVisibleChanged">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0,0,15,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Comparison Module" FontSize="24" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Left"/>

                <Button Content="❓ Help &amp; Docs" Background="Transparent" Foreground="#3498DB" BorderThickness="0" Cursor="Hand" HorizontalAlignment="Right" VerticalAlignment="Center" FontWeight="SemiBold" Click="BtnHelp_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <TextBlock Text="{TemplateBinding Content}" TextDecorations="Underline"/>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="20" BorderBrush="#DCDDE1" BorderThickness="1" Margin="0,0,0,20">
                <StackPanel>

                    <Border Background="#F0F8FF" CornerRadius="5" Padding="15" BorderBrush="#3498DB" BorderThickness="1" Margin="0,0,0,20">
                        <StackPanel>
                            <TextBlock Text="💡 Smart Baseline Matcher" FontWeight="Bold" Foreground="#2980B9" Margin="0,0,0,5" FontSize="15"/>
                            <TextBlock Text="Select the target environment and your baseline file. The system will load your most recent extraction for the target file." Foreground="#34495E" Margin="0,0,0,10" TextWrapping="Wrap"/>

                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="130"/>
                                    <ColumnDefinition Width="15"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <ComboBox x:Name="CmbSmartEnv" Grid.Column="0" Padding="8" FontSize="14" BorderBrush="#3498DB" SelectionChanged="SmartMatcher_Changed" Background="White" SelectedIndex="0">
                                    <ComboBoxItem Content="D000 (D)" Tag="D"/>
                                    <ComboBoxItem Content="Q000 (Q)" Tag="Q"/>
                                </ComboBox>

                                <ComboBox x:Name="CmbBaselineSelector" Grid.Column="2" Padding="8" FontSize="14" BorderBrush="#3498DB" SelectionChanged="SmartMatcher_Changed" Background="White"/>
                            </Grid>

                            <StackPanel Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
                                <Button x:Name="BtnAddBaselineComp" Content="➕ Add Baseline" Click="BtnAddBaseline_Click" Background="#9B59B6" Foreground="White" Padding="10,5" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0" FontWeight="SemiBold">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                                <Button x:Name="BtnOpenBaselineComp" Content="📂 Open Folder" Click="BtnOpenBaselineFolder_Click" Background="#3498DB" Foreground="White" Padding="10,5" BorderThickness="0" Cursor="Hand" FontWeight="SemiBold">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}" CornerRadius="4"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <TextBlock Text="Or select the base and target CSV files manually below:" Margin="0,0,0,15" Foreground="#7F8C8D" TextWrapping="Wrap"/>

                    <TextBlock Text="Base Extraction File:" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#2C3E50"/>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtBaseFile" IsReadOnly="True" Padding="8" FontSize="14" BorderBrush="#DCDDE1" Margin="0,0,10,0" Background="#F9F9F9"/>
                        <Button Content="Browse..." Grid.Column="1" Padding="15,8" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnBrowseBase_Click"/>
                    </Grid>

                    <TextBlock Text="Target Extraction File (to compare against):" Margin="0,0,0,5" FontWeight="SemiBold" Foreground="#2C3E50"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="TxtTargetFile" IsReadOnly="True" Padding="8" FontSize="14" BorderBrush="#DCDDE1" Margin="0,0,10,0" Background="#F9F9F9"/>
                        <Button Content="Browse..." Grid.Column="1" Padding="15,8" Background="#34495E" Foreground="White" BorderThickness="0" Cursor="Hand" Click="BtnBrowseTarget_Click"/>
                    </Grid>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="BtnOpenComparisonFolder" Content="📁 Open Report Folder" Click="BtnOpenFolder_Click" Background="#3498DB" Foreground="White" Padding="15,10" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Margin="0,0,10,0">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button x:Name="BtnRunComparison" Content="🚀 Run Deep Comparison" Click="BtnRunComparisonAction_Click" Style="{DynamicResource ActionButtonStyle}" Width="240" IsEnabled="False"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Row="2" Margin="0,10,0,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Border x:Name="PanelDashboard" Grid.Row="0" Background="White" CornerRadius="8" Padding="20" BorderBrush="#DCDDE1" BorderThickness="1" Margin="0,0,0,15" Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Grid Grid.Column="0" Width="140" Height="140" Margin="0,0,30,0">
                            <Ellipse Stroke="#ECF0F1" StrokeThickness="12" Fill="Transparent"/>
                            <Ellipse x:Name="CircleProgress" Stroke="#2ECC71" StrokeThickness="12" Fill="Transparent" StrokeDashCap="Round" RenderTransformOrigin="0.5,0.5">
                                <Ellipse.RenderTransform>
                                    <RotateTransform Angle="-90"/>
                                </Ellipse.RenderTransform>
                            </Ellipse>
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock x:Name="TxtScorePercentage" Text="0%" FontSize="32" FontWeight="Bold" Foreground="#2C3E50" HorizontalAlignment="Center"/>
                                <TextBlock Text="Match" FontSize="14" Foreground="#7F8C8D" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Grid>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtDashboardTitle" Text="Analysis successfully completed!" FontSize="20" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,15" TextWrapping="Wrap"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Rows Analyzed" FontSize="13" Foreground="#7F8C8D"/>
                                    <TextBlock x:Name="TxtTotalRows" Text="0" FontSize="22" FontWeight="Bold" Foreground="#34495E"/>
                                </StackPanel>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Errors Found" FontSize="13" Foreground="#7F8C8D"/>
                                    <TextBlock x:Name="TxtTotalErrors" Text="0" FontSize="22" FontWeight="Bold" Foreground="#E74C3C"/>
                                </StackPanel>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <DataGrid x:Name="GridResults" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
                          Background="White" BorderBrush="#DCDDE1" BorderThickness="1" HeadersVisibility="Column"
                          RowBackground="White" AlternatingRowBackground="#F9F9F9" GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#ECF0F1" Visibility="Collapsed"
                          ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2C3E50"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Padding" Value="5"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Status" Width="80">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding StatusColor}" CornerRadius="4" Padding="5,2" Margin="5" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding StatusIcon}" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Test ID &amp; Table" Binding="{Binding TableName}" Width="200" FontWeight="SemiBold"/>
                        <DataGridTemplateColumn Header="Details" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding ErrorDetails}" TextWrapping="Wrap" Foreground="#7F8C8D" Margin="5"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock x:Name="TxtComparisonWaiting" Grid.Row="1" Text="Select your files and run the comparison."
                           Foreground="#BDC3C7" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>